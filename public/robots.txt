User-agent: Twitterbot
Disallow:

User-agent: facebookexternalhit
Disallow:

User-agent: *
Allow: /$
Allow: /
Disallow: /

# UNOBRAVO: upstream ends this file with a `Sitemap:` directive pointing at
# Excalidraw's sitemap. That is the same instruction as the `rel="canonical"`
# removed from index.html — that this deployment is Excalidraw — and a crawler
# reads it first. The `Allow: /sitemap.xml` line went with it: this build emits
# no sitemap.
